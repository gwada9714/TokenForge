import { createTheme } from "@mui/material/styles";

export const testTheme = createTheme({
  // Thème minimal pour les tests
  palette: {
    primary: {
      main: "#000000",
    },
    secondary: {
      main: "#666666",
    },
  },
});
