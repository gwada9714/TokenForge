import { Box, Typography } from "@mui/material";

const Marketplace = () => {
  return (
    <Box>
      <Typography variant="h4" style={{ color: "#182038" }}>
        Marketplace
      </Typography>
      <Typography variant="body1" style={{ color: "#666" }}>
        Explore the marketplace to buy and sell tokens!
      </Typography>
    </Box>
  );
};

export default Marketplace;
