import { Box, Typography } from '@mui/material';

const MyTokens = () => {
  return (
    <Box>
      <Typography variant="h4" style={{ color: '#182038' }}>
        My Tokens
      </Typography>
      <Typography variant="body1" style={{ color: '#666' }}>
        Here you can view your tokens!
      </Typography>
    </Box>
  );
};

export default MyTokens;
