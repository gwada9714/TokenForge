import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminTokens: React.FC = () => {
  return (
    <Box p={4}>
      <Typography variant="h4" component="h1" gutterBottom>
        Gestion des Tokens
      </Typography>
      {/* Add token management content */}
    </Box>
  );
};

export default AdminTokens;
