import React from 'react';
import { Box, Typography } from '@mui/material';

const StakingService: React.FC = () => {
  return (
    <Box p={4}>
      <Typography variant="h4" component="h1" gutterBottom>
        Service de Staking
      </Typography>
      {/* Add staking service content */}
    </Box>
  );
};

export default StakingService;
